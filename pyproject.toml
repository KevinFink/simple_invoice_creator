[project]
name = "simple-invoice-creator"
version = "0.1.0"
description = "Generate PDF invoices"
requires-python = ">=3.13"
dependencies = [
    "reportlab>=4.0.0",
]

[project.scripts]
create-invoice = "create_invoice:main"
