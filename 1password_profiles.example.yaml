# 1Password profile configurations
# Copy this file to 1password_profiles.yaml and fill in your details.
# Each top-level key is a profile name you can reference with --profile.

acme:
  vault: Private
  title: invoice-config-acme
  account: my.1password.com
  config_path: config.toml

globex:
  vault: Work
  title: invoice-config-globex
  account: company.1password.com
  config_path: config_globex.toml
